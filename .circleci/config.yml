---
version: 2.1

orbs:
  architect: giantswarm/architect@4.36.0

workflows:
  build:
    jobs:
     - architect/push-to-registries:
        context: architect
        name: push-to-registries
        image: "giantswarm/loki-multi-tenant-proxy-gs"
        dockerfile: "build/package/Dockerfile-with-tests"
        filters:
          tags:
            only: /^v.*/
